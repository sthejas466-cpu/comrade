[phases.setup]
nixPkgs = ["python3", "gcc", "gnumake", "pkg-config", "binutils"]

[phases.install]
cmds = ["npm install --build-from-source"]

[variables]
NIXPACKS_NODE_VERSION = "22"
# This tells the compiler to use less memory
MAKEFLAGS = "-j1"